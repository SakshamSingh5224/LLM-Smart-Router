httpx>=0.27
python-dotenv>=1.0
pandas>=2.0
pyarrow>=15.0
datasets>=2.19

# Phase 2 - classifier
scikit-learn>=1.3
scipy>=1.11
joblib>=1.3
fastembed>=0.3   # optional embedding backend; tfidf backend works without it

# Phase 3 - gateway + router service
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Phase 4 - observability, rate limiting, load testing
prometheus-fastapi-instrumentator>=7.0
locust>=2.24
